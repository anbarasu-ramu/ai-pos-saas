# Optional shared defaults copied into `.env` for values reused across dev and prod.
KEYCLOAK_REALM=ai-pos
KEYCLOAK_CLIENT_ID=pos-frontend
POSTGRES_DB=pos
POSTGRES_USER=user
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

